import 'package:flutter/material.dart';

class _GridTitleText extends StatelessWidget {
  const _GridTitleText({Key? key, required this.text}) : super(key: key);
  final String text;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
